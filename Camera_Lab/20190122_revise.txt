**************关于Camera程序的代码修改部分*******************

在之前与Xiaocheng讨论的基础上将代码进行了部分修改：
	1.原先是需要确保每一次写入的excel表的数据与之前写入的excel表数据完全不重复，
	确保每次写入的都是新的数据，现在做的修改就是可以每次在一个表中进行操作，只需要将
	要写入的数据累加到该表中，该表的其他格式都不能更改，只确保存入新的数据。
	2.也可以按照之前的方式，保证每次写入的数据都是之前没有写过的，表中格式不更改。

